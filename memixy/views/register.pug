extends layout

block content
  form(method='POST' action='/register')
    div.form-group
      label(for='username') Nazwa użytkownika (tylko angielskie znaki i liczby):
      br
      input.form-control(type='text', name='username')
    div.form-group
      label(for='password') Hasło:
      br
      input.form-control(type='text', name='password')
    button(type='submit') ZAREJESTRUJ
